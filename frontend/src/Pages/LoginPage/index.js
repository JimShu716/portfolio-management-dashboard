import './styles.css';

const Login = (props) =>{

    // useEffect(()=>{
    //     axios.get(process.env.REACT_APP_HOST + '/1', ).then(r => {
    //         console.log(r)
    //     });
    // }, [])

    return (
        <div>
        </div>
    )
}

export default Login;